export const API_TYPE = 'api';
export const API_RESULT = 'api_result';

export const ApiResults = Object.freeze({
  SUCCESS: 'success',
  ERROR: 'error',
});

export const Endpoints = Object.freeze({
  AUTH: '/token',
});
